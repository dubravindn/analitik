# TEST_REPORT — этап 0

`py_compile hermes/*.py` — чисто. `pytest` — **24 passed** (0.07 c).

## Что покрыто

**test_calc.py** (9) — расчётные формулы на известных числах (сверка с 2026-08-04):
прибыль от продаж, маржа %, средний чек, деление на ноль, коп→руб, дельта %,
ценовые правила.

**test_msk.py** (4) — пункт 12, таймзоны:
- сдвиг MSK = +3ч; `msk_now()` возвращает aware-время +3ч;
- известная конверсия: 18:47 МСК (МойСклад) → 15:47 UTC;
- `msk_today()` = дата `msk_now()`.

**test_clients_netting.py** (4) — пункт 13, нетто-суммы (фейковый conn):
- demand пишется с `+sum_kop`, `doc_type='demand'`;
- возврат пишется с `-sum_kop`, `doc_type='salesreturn'` (40 ₽ → −4000);
- demand +4000 и возврат −4000 → нетто 0;
- **ограничение детектора оттока задокументировано**: он не отличает реальную
  паузу от дыры из-за отсутствия синка; полностью в SQL без БД-фикстуры не
  решается. Тест проверяет наличие митигации (etl_clients тянет demand+salesreturn,
  отток считает только по `doc_type='demand'`; daily синкает клиентов ежедневно,
  backfill закрывает историю).

**test_group_filter.py** (3) — пункт 0.5:
- `_group_filter(None)` → пустой фильтр;
- `_group_filter('СРЕЗКА')` (значение из PDF-контекста) → применяется
  `folder_path LIKE 'Ассортимент/%'` + `SPLIT_PART=СРЕЗКА`;
- число `%s` совпадает с числом параметров.

**test_telegram_split.py** (4) — пункт 1.4:
- короткий текст → одна часть;
- длинный текст режется по границам строк, склейка через `\n` восстанавливает
  исходник (строки не разрезаны);
- одиночная сверхдлинная строка режется жёстко;
- смесь нормальных и сверхдлинных строк — все части ≤ лимита.

## Чего НЕ покрывают юнит-тесты (нужна БД-фикстура)

- SQL-запросы отчётов (report_*), детектор оттока на реальных интервалах,
  ETL- upsert в PostgreSQL. Проверялись вручную на боевых данных при разработке
  (см. коммиты этапа 0). Полноценные интеграционные тесты с временной БД —
  кандидат на отдельную задачу.
